# pixel-level-nitrate-sources-calculation
# Code execution order
# (1) isotopes image convert to matrix for SIAR model
# (2) Calculate the pixel-level contribution rate each year in a loop
# (3) SIAR CSV results conver to geotiff
# You need to check if the corresponding libraries are installed.
